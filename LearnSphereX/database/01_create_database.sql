-- LearnSphereX database bootstrap
-- Run this once in MySQL. Spring Boot/Hibernate will create/update the tables.

CREATE DATABASE IF NOT EXISTS learnspherex_db
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE learnspherex_db;
